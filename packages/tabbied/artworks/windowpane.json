{
  "name": "Windowpane",
  "slug": "windowpane",
  "galleryOrder": 285,
  "svgExportNote": "Cells that mix two border widths on rounded corners can differ from the screen by up to a pixel where the arcs meet.",
  "description": "A lattice of mullions with the odd crossing node, re-weighting on reseed.",
  "palette": [
    "#F2E9DC",
    "#E63329",
    "#1D3D8F",
    "#F0C02E",
    "#1A1A1A"
  ],
  "colors": {
    "min": 2,
    "max": 5,
    "default": 5
  },
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "10x15",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 1,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--rule: ( margin: -0.5px; @random(${shapeFrequency}) { border-top: @pick(6px, 10px) solid @p(var(--color1), var(--color2), var(--color3), var(--color4)); } @random(${shapeFrequency}) { border-left: @pick(6px, 10px) solid @p(var(--color1), var(--color2), var(--color3), var(--color4)); } @random(0.18) { :after { content: ''; position: absolute; @size: 26%; left: 37%; top: 37%; border-radius: @pick(0, 50%); background: @p(var(--color1), var(--color2), var(--color3), var(--color4)); -webkit-transition: ease 400ms; transition: ease 400ms; } } -webkit-transition: ease 400ms; transition: ease 400ms; );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box } :container { background: var(--color0); overflow:hidden; }"
  }
}

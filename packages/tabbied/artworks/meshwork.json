{
  "name": "Meshwork",
  "slug": "meshwork",
  "galleryOrder": 1532,
  "description": "The same mesh drawn heavy, so the openings become the pattern.",
  "palette": [
    "#FFFFFF",
    "#06AED5",
    "#086788",
    "#F0C808",
    "#DD1C1A",
    "#4C4C4C"
  ],
  "colors": {
    "min": 2,
    "max": 6,
    "default": 6
  },
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "6x9",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 1,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--rule: ( @random(${shapeFrequency}) { background: @p(var(--color1), var(--color2), var(--color3), var(--color4), var(--color5)); -webkit-mask: repeating-linear-gradient(0deg, #000 0 15%, transparent 15% 20%), repeating-linear-gradient(90deg, #000 0 15%, transparent 15% 20%); mask: repeating-linear-gradient(0deg, #000 0 15%, transparent 15% 20%), repeating-linear-gradient(90deg, #000 0 15%, transparent 15% 20%); } -webkit-transition: ease 450ms; transition: ease 450ms; );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box } :container { background: var(--color0); overflow:hidden; }"
  }
}

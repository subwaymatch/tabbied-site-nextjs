{
  "name": "Ortho",
  "slug": "ortho",
  "galleryOrder": 1016,
  "galleryWhite": true,
  "description": "No perspective at all — the same square everywhere, and only the weight of the line changing.",
  "tags": [
    "squares",
    "grid",
    "lines"
  ],
  "mood": [
    "technical",
    "calm"
  ],
  "density": "medium",
  "goodFor": [
    "card-texture",
    "wallpaper",
    "hero-background"
  ],
  "palette": [
    "#11151A",
    "#FF5252",
    "#FFB300",
    "#26C6DA",
    "#66BB6A",
    "#EEEEEE"
  ],
  "colors": {
    "min": 2,
    "max": 6,
    "default": 6
  },
  "sizing": {
    "minCellPx": 30
  },
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "8x12",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 1,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--rule: ( @random(${shapeFrequency}) { :after { content: ''; position: absolute; inset: 6%; border-style: solid; border-color: @p(var(--color1), var(--color2), var(--color3), var(--color4), var(--color5)); border-width: calc(@calc(1 + 7 * @y / @Y) * 1px * 6 / @size-col); box-sizing: border-box; -webkit-transition: ease 450ms; transition: ease 450ms; } } -webkit-transition: ease 450ms; transition: ease 450ms; );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box } :container { background: var(--color0); overflow:hidden; }"
  }
}

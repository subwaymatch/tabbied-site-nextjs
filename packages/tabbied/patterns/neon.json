{
  "name": "Neon",
  "slug": "neon",
  "galleryOrder": 80,
  "svgExportNote": "The tube glow is exported as SVG drop-shadow filters. Browsers render them correctly, but some design tools (like Figma or Illustrator) import SVG filters imperfectly.",
  "galleryWhite": true,
  "description": "Glowing tube segments rearranging themselves on a dark wall - each redraw re-aims the lights and re-mixes their halos.",
  "tags": [
    "lines",
    "grid"
  ],
  "mood": [
    "bold",
    "festive"
  ],
  "density": "sparse",
  "goodFor": [
    "hero-background",
    "poster",
    "og-image",
    "wallpaper"
  ],
  "palette": [
    "#14101F",
    "#FF2E97",
    "#2EE6FF",
    "#B14EFF",
    "#45FFA2"
  ],
  "colors": {
    "min": 2,
    "max": 5,
    "default": 5
  },
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "6x9",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 0.8,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--ang: @p(0deg, 45deg, 90deg, 135deg); --rule: ( /* tube and both halos share one emitted color */ --tube: @p(var(--color1), var(--color2), var(--color3), var(--color4)); @random(${shapeFrequency}) { transform: rotate(var(--ang)); :after { content: ''; position: absolute; left: 14%; top: 47%; width: 72%; height: 6%; border-radius: 99px; background: @var(--tube); box-shadow: 0 0 calc(80px / @Y) @var(--tube), 0 0 calc(200px / @Y) @var(--tube); transition: ease 500ms; } } transition: ease 500ms; );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box } :container { background: var(--color0); overflow:hidden; }"
  }
}

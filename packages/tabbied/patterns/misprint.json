{
  "name": "Misprint",
  "slug": "misprint",
  "galleryOrder": 128,
  "svgExportNote": "The overlapping inks use a blend mode. Browsers render it correctly, but some design tools import SVG blend modes imperfectly.",
  "description": "Two riso plates sliding in and out of register — mirrored offsets pull the inks apart and let them overlap into new blends.",
  "tags": [
    "dots",
    "circles",
    "grid"
  ],
  "mood": [
    "retro",
    "playful"
  ],
  "density": "medium",
  "goodFor": [
    "packaging",
    "textile",
    "wallpaper"
  ],
  "palette": [
    "#FBF7EE",
    "#FF4D6D",
    "#2E86AB",
    "#F5B82E",
    "#7E4A8C"
  ],
  "colors": {
    "min": 2,
    "max": 5,
    "default": 5
  },
  "options": [
    {
      "id": "grid",
      "displayName": "Columns and rows",
      "type": "ButtonSelectGroup",
      "default": "6x9",
      "options": [
        "2x3",
        "4x6",
        "6x9",
        "8x12",
        "10x15"
      ],
      "replace": "${grid}"
    },
    {
      "id": "frequency",
      "displayName": "Frequency",
      "type": "Slider",
      "default": 0.9,
      "min": 0.2,
      "max": 1,
      "step": 0.1,
      "replace": "${shapeFrequency}"
    }
  ],
  "code": {
    "style": "--plate1: @p(var(--color1), var(--color3)); --plate2: @p(var(--color2), var(--color4)); --rule: ( /* the two plates share one emitted offset, mirrored, so they slide apart symmetrically */ --mx: @r(-10, 10); --my: @r(-10, 10); @random(${shapeFrequency}) { :before { content: ''; position: absolute; @size: 58%; left: calc(21% + @var(--mx) * 1%); top: calc(21% + @var(--my) * 1%); border-radius: 50%; background: var(--plate1); opacity: 0.72; mix-blend-mode: multiply; transition: ease 700ms; } :after { content: ''; position: absolute; @size: 58%; left: calc(21% - @var(--mx) * 1%); top: calc(21% - @var(--my) * 1%); border-radius: 50%; background: var(--plate2); opacity: 0.72; mix-blend-mode: multiply; transition: ease 700ms; } } );",
    "doodle": ":doodle { @grid: ${grid}; @size: ${width} ${height}; overflow:hidden; text-align:center; box-sizing:border-box } :container { background: var(--color0); overflow:hidden; }"
  }
}

# Superseded

This was the pre-flight plan for generating mockups that embed real Tabbied
patterns. The workflow has since been built and piloted, so the authoritative
document is now:

**`docs/pattern-mockups.md`**

It carries the verified API details (including the `input_fidelity` rejection
on `gpt-image-2`), the render-then-edit pipeline, the fidelity tiers, and
measured results for which designs survive the model's redraw.
